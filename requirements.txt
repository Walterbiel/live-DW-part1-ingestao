# =======================================
# Orquestração
apache-airflow[postgres]==2.8.4          # pendulum 3+ já OK com Py3.11

# ---------------------------------------
# Conexão PostgreSQL (já incluída no extra [postgres], mas explícita não faz mal)
psycopg2-binary==2.9.9

# ---------------------------------------
# ORM
SQLAlchemy>=1.4,<2.0                    # Airflow 2.x não suporta SQLAlchemy 2

# ---------------------------------------
# Leitura Excel
openpyxl>=3.1,<3.3

# ---------------------------------------
# API
fastapi>=0.110,<0.113
uvicorn[standard]>=0.27,<0.29
